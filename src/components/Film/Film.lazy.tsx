import React, { lazy, Suspense } from 'react';

// const LazyFilm = lazy(() => import('./Film'));

// const Film = (props: JSX.IntrinsicAttributes & { children?: React.ReactNode; }) => (
//   <Suspense fallback={<div>...loading</div>}>
//     <LazyFilm {...props} />
//   </Suspense>
// );

export {};
